apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: matchmaking-db-pvc
  labels:
    app.kubernetes.io/name: ft-transcendence
    app.kubernetes.io/part-of: ft-transcendence
    app.kubernetes.io/component: matchmaking
    app.kubernetes.io/managed-by: kustomize
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  # StorageClassName sera défini dans les overlays selon l'environnement
